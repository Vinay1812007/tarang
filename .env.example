# Optional: comma-separated API base override list. When unset, the four
# built-in bases in src/constants/endpoints.ts are used.
# VITE_API_BASES=https://saavn.dev/api,https://saavn.sumit.co/api

# Optional: app display name override (used by About page).
# VITE_APP_NAME=Tarang

# No secrets belong in this file or anywhere in the frontend.
